# ─── Supabase Postgres ────────────────────────────────────────────────
# Settings -> Database -> Connection string -> "URI" (con session pooler)
DATABASE_URL=postgres://postgres.[project-ref]:[password]@aws-0-[region].pooler.supabase.com:6543/postgres
# Para drizzle-kit migrate (conexion directa, no pooler)
DATABASE_URL_DIRECT=postgres://postgres.[project-ref]:[password]@aws-0-[region].pooler.supabase.com:5432/postgres

# Para uso desde el cliente si llega a hacer falta (lectura publica)
NEXT_PUBLIC_SUPABASE_URL=https://[project-ref].supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Solo server-side. NUNCA exponer al cliente.
SUPABASE_SERVICE_ROLE=

# ─── Cerebras (LLM) ────────────────────────────────────────────────────
# https://cloud.cerebras.ai/ -> API Keys
CEREBRAS_API_KEY=
CEREBRAS_BASE_URL=https://api.cerebras.ai/v1
CEREBRAS_MODEL=llama3.1-8b

# Fallback opcional (Groq, Gemini, OpenRouter)
LLM_FALLBACK_API_KEY=
LLM_FALLBACK_BASE_URL=
LLM_FALLBACK_MODEL=

# ─── Cron ──────────────────────────────────────────────────────────────
# Header Authorization: Bearer <CRON_SECRET> requerido en /api/cron/*
CRON_SECRET=

# ─── App ───────────────────────────────────────────────────────────────
NEXT_PUBLIC_APP_URL=http://localhost:3000
# Origen publico (sin slash final). Lo usan metadata, sitemap, robots y RSS.
# En Vercel se rellena con tu dominio (https://kernelia.app, etc).
NEXT_PUBLIC_SITE_URL=http://localhost:3000
NODE_ENV=development

# ─── Admin backoffice (Fase 7) ─────────────────────────────────────────
# Email del primer admin. Se siembra solo si la tabla users esta vacia
# (idempotente). En produccion debe ser el email del operador humano.
INITIAL_ADMIN_EMAIL=

# Secreto para firmar la cookie de sesion (HMAC-SHA256).
# Generar con: openssl rand -hex 32
# Minimo 32 caracteres; fail-fast en boot si falta o es mas corto.
SESSION_SECRET=

# Resend — envia los enlaces de restablecimiento de contrasenya (login es
# por email+password directamente; magic-link fue retirado en 7.F).
# Crear en https://resend.com -> API Keys.
RESEND_API_KEY=
# Remitente. Hasta verificar el dominio kernelia.dev en Resend (SPF+DKIM),
# se puede usar "Kernelia <onboarding@resend.dev>" pero solo entrega al
# email de la cuenta de Resend. En produccion: "Kernelia <admin@kernelia.dev>".
EMAIL_FROM=

# ─── Broadcast distribution (Fase 8.A) ─────────────────────────────────
# Toggle global. Si vale "false"/"0" el cron de broadcast salta el ciclo
# sin tocar DB ni APIs externas. Util para pausar publicaciones sin tener
# que redeployar.
BROADCAST_ENABLED=false
# Umbral de relevance_score (LLM) para que un articulo se publique en las
# redes. Valores tipicos 0.6 (mucho volumen) - 0.85 (muy selectivo). Por
# defecto 0.75. Articulos clasificados antes de la migracion 0005 tienen
# relevance_score=NULL y nunca pasan el filtro -> cero flood de backlog.
BROADCAST_MIN_RELEVANCE_SCORE=0.75

# Mastodon — Settings > Development > New Application
# Scope minimo: write:statuses
MASTODON_INSTANCE_URL=https://fosstodon.org
MASTODON_ACCESS_TOKEN=

# Bluesky — Settings > Privacy and security > App passwords (NO la del login)
BLUESKY_IDENTIFIER=kernelia.dev
BLUESKY_APP_PASSWORD=

# Telegram — @BotFather -> /newbot. Anyade el bot como admin con permiso
# "Post Messages" al channel objetivo.
TELEGRAM_BOT_TOKEN=
# Username del channel (con @) o id numerico negativo.
TELEGRAM_CHAT_ID=@kernelia
